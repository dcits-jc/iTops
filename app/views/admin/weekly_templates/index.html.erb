<%#= link_to('新建周报模板',new_admin_weekly_template_path) %>


<table class="table">
  <thead>
    <tr>
      <td>日期</td>
    </tr>
  </thead>
  <tbody>
    <% @weekly_templates.each do |weekly_template| %>
    <tr>
      <td><%= link_to 'Y'+weekly_template.year.to_s+'-W'+weekly_template.week.to_s,admin_weekly_template_path(weekly_template) %></td>
    </tr>
    <% end %>
  </tbody>
</table>