

<div class="row">

  <% @weeklies.each do |weekly| %>
    <% if weekly.start_time<Time.new and weekly.end_time>Time.new %>
    <% else %>
      <div class="col-md-2 btn btn-default">
        <p><%= link_to('Y'+(weekly.year).to_s+'W'+(weekly.week).to_s,weekly_path(weekly))  %></p>
      </div>
    <% end %>
  <% end %>
  
</div>

